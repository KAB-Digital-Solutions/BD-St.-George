/**
 * Central member registry — core fixed columns plus dynamic fields via {@code formengine}.
 * <p>
 * {@code clergy_id} is stored now; FK validation comes when the {@code clergy} module is built.
 */
package com.example.backend.members;
