/**
 * Family / household members linked to a {@code members} record.
 * Relationship type (spouse, child, etc.) is stored via the form engine ({@code family_members} module key).
 */
package com.example.backend.family;
