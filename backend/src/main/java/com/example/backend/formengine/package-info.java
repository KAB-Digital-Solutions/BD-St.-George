/**
 * Dynamic form engine — admin-configurable field definitions and per-record custom values.
 * <p>
 * Admin configures forms via {@code /api/admin/form-fields}.
 * Domain modules store values via {@code /api/{moduleKey}/{recordId}/custom-fields}.
 */
package com.example.backend.formengine;
